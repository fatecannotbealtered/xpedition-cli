; `npm version` only edits files (package.json + lockfile) and runs the
; `version` lifecycle hook (scripts/sync-version.js). It must NOT auto-commit or
; auto-tag: the release flow is push -> CI green -> tag, so the human reviews the
; synced diff and tags after CI passes.
git-tag-version=false
